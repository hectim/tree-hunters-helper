<!DOCTYPE html>
<html lang="en">
  <head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<link href="css/bootstrap.min.css" rel="stylesheet">
		<link rel="stylesheet" href="font-awesome/css/font-awesome.min.css">
		<script src= "http://ajax.googleapis.com/ajax/libs/angularjs/1.3.14/angular.min.js"></script>
		<script type="text/javascript" language="javascript" src="calendar.js"></script>
		<style>
		#card{
			border-radius:25px;
    		border:2px solid #333333;
			height:50%;
			text-align:center;
			box-shadow: 10px 10px 5px #888888;
		}
		span {
			font-size: 5em;
			position:relative;
			top:35%
		}
		#newDeck{
			border-radius:3px;
			border:2px solid #333333;
			height:50%;
			box-shadow: 10px 10px 5px #888888;
		}
		#deckList{
			border-radius:3px;
			border:2px solid #333333;
			height:50%;
			overflow-y:auto;
			box-shadow: 5px 10px 5px #888888;
			padding-bottom:5px;
			padding-top:5px;
		}
		#content{
			height:100vh;
			width:100%;
			-moz-user-select: -moz-none;
			-khtml-user-select: none;
			-webkit-user-select: none;
		}
		#fa{
			font-size:5em;
		}
		#fab{
			font-size:5em;
			margin-left:10px;
		}
		#eachCard{
			border-bottom:2px solid #333333;
			overflow:hidden;
			text-overflow:ellipsis;
			white-space: nowrap;   		
		}
		#eachCard a{
			float:right;
			margin-top:10px;		
		}
		a{
			color:#333333;
		}
		.vertical-center {
		  min-height:100%;
		  min-height:100vh;
		  display:flex;
		  align-items:center;
		}
		.cardBack{
			background-color:#cccccc;
		}
		textarea{
			resize:none;
		}
		#makeRight{
			float:right;
		}
		#frontPage{
			border-radius:10px;
			border:2px solid #333333;
			text-indent:5px;
		}
		#frontPage a{
			float:right;
			font-size:2em;
			margin-right:5px;
			margin-top:-2.5%;
		}
		#fix{
			min-width:8.33%;
		}
		</style>
	</head>
	<body>
		<div id="content" ng-app="flash" ng-controller="menu" class="vertical-center">
			<div class="col-md-6 col-md-offset-3" ng-show="cardDisplayed===0">
				<div id="frontPage" ng-repeat="deck in decks">
					<h3>{{decks[$index].name}}
					<a><i id="makeRight" class="fa fa-sign-out" ng-click="cardView($index)"></i></a>
					<a><i id="makeRight" class="fa fa-pencil" ng-click="deckView($index)"></i></a>
					</h3>
				</div>
			</div>
	
			<div id="fa" class="col-md-1" ng-show="cardDisplayed===1">
				<center>
				<a><i class="fa fa-home" ng-click="homeView()"></i></a>
				<div id="fix">{{score}}</div>
			</div>
			<div id="card" class="col-md-10" ng-controller="card" ng-click="clickOn($event)" ng-show="cardDisplayed===1"  ng-class="{true:'', false:'cardBack'}[front]">
				<span>{{getContent()}}</span>
			</div>
	
			<div id="fab" class="col-md-1" ng-show="cardDisplayed===1" ng-controller="card">
				<a><i class="fa fa-plus-square" ng-click="right()"></i></a>
				<a><i class="fa fa-minus-square" ng-click="wrong()"></i></a>
			</div>

			<div id="deckList" class="col-md-2 col-md-offset-1" ng-controller="card" ng-click="clickOn($event)" ng-show="cardDisplayed===2">
				<div id="eachCard" ng-repeat="n in getDeck()">
						Front: {{n.front}}<a><i class="fa fa-trash" ng-click="deleteCard($event, $index)"></i></a><br />
						Back:  {{n.back}}
				</div>
			</div>
			<div id="newDeck" class="col-md-8" ng-controller="card" ng-click="clickOn($event)" ng-show="cardDisplayed===2">
				<h3><center>Add Card to the {{decks[currentDeck].name}} Deck</center></h3>
				<label>Front:</label>
				<textarea class="form-control" rows="5" id="front" ng-model="first.value"></textarea>
				<label>Back:</label>
				<textarea class="form-control" rows="5" id="back" ng-model="second.value"></textarea>
				<br />
				<center><button type="submit" ng-click="addCard()">Submit</button></center>
			</div>
	
			<div id="fa" class="col-md-1" ng-show="cardDisplayed===2">
				<a><i class="fa fa-check-circle" ng-click="homeView()"></i></a>
			</div>
		</div>
	</body>
</html>
